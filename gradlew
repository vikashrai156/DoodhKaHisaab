#!/usr/bin/env sh
# Minimal gradlew placeholder. This repo's CI uses gradle action, and local users should run Gradle using Android Studio.
echo "This gradlew is a placeholder. Use Android Studio or install Gradle to run builds locally."
exit 0
